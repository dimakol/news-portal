1. run npm install on the root folder of the node project.

For production mode:
2. run "node server.js" (Node.js should be installed on the system) -> You should see: Server running at: http://localhost:5000/

For development mode:
2. run "npx nodemon server.js" locally -> You should see: Server running at: http://localhost:5000/
